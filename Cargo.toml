[package]
name = "code"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"
license = "MIT OR Apache-2.0"
description = "A native coding environment built with Zed's rendering framework and Once"
repository = "https://github.com/tuist/code"

[dependencies]
gpui = "0.2.2"
